const double kMobileBreakpoint = 600.0; // Example: screens <= 600px are mobile
const double kTabletBreakpoint = 900.0; // Example: screens between 601 and 900 are tablet
const double kDesktopBreakpoint = 1200.0; // Example: screens >= 901 are desktop (or whatever your tiers are)